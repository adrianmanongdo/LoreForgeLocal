from pathlib import Path
r=Path("NullSignalMVP/app/src/main/java/com/nullsignal/local")
p=r/"ActivityTransitionReceiver.java";s=p.read_text();s=s.replace("ParkingInference.onTransition(context,m,enter);","TransitInference.onTransition(context,m,enter);");s=s.replace("private static String pretty(String s){return s.replace('_',' ');}","private static String pretty(String s){if(\"in_vehicle\".equals(s))return \"motorized public-transit movement\";return s.replace('_',' ');}");p.write_text(s)
p=r/"MobilityEngine.java";s=p.read_text();s=s.replace('String msg="Your current route toward "+placeName(ms.getPlace(dest))+" has remained outside the route corridor learned from previous trips.";','String msg="Your route toward "+placeName(ms.getPlace(dest))+" has stayed outside your usual commute corridor. If you are on public transport, you may be on a different or wrong route.";');s=s.replace('state.putState("mob_current_journey",String.valueOf(id));state.putState("mob_route_off_count","0");','state.putState("mob_current_journey",String.valueOf(id));state.putState("mob_route_off_count","0");state.putState("journey_transfer_count","0");');s=s.replace('public static String travelMode(String motion,float speed){if("in_vehicle".equals(motion)||speed>7)return "vehicle";if("on_bicycle".equals(motion)||speed>3)return "bicycle";if("running".equals(motion)||speed>2)return "running";if("walking".equals(motion)||speed>.5)return "walking";return "unknown";}','public static String travelMode(String motion,float speed){if("in_vehicle".equals(motion)||speed>7)return "public transit";if("on_bicycle".equals(motion)||speed>3)return "bicycle";if("running".equals(motion)||speed>2)return "running";if("walking".equals(motion)||speed>.5)return "walking";return "unknown";}');s=s.replace('        MobilityStore.LocationRow row=new MobilityStore.LocationRow','        checkApproachAlert(c,lat,lon,now);\n\n        MobilityStore.LocationRow row=new MobilityStore.LocationRow');s=s.replace('    public static void mobilityAlert(Context c,String type,String message,String key)', '    private static void checkApproachAlert(Context c,double lat,double lon,long now){NullSignalDb st=NullSignalDb.get(c);long pid=parseLong(st.getState("approach_target_place","0"));if(pid<=0)return;MobilityStore.PlaceRow p=MobilityStore.get(c).getPlace(pid);if(p==null){st.putState("approach_target_place","0");return;}double radius=parseDouble(st.getState("approach_radius_m","800"));if(Double.isNaN(radius)||radius<100)radius=800;double d=distanceMeters(lat,lon,p.lat,p.lon);st.putState("approach_distance_m",String.valueOf(Math.round(d)));double closest=parseDouble(st.getState("approach_closest_m","NaN"));if(Double.isNaN(closest)||d<closest){closest=d;st.putState("approach_closest_m",String.valueOf(d));}if(d<=radius){mobilityAlert(c,"approach_target:"+pid,"You are about "+Math.round(d)+" m from "+placeName(p)+". This is your wake/get-off alert.","approach:"+pid+":"+now);st.putState("approach_target_place","0");st.putState("approach_distance_m","0");st.putState("approach_closest_m","");st.putState("approach_overshoot_alerted","0");}else if(closest<Math.max(1800,radius*2.5)&&d>closest+650&&!"1".equals(st.getState("approach_overshoot_alerted","0"))){mobilityAlert(c,"possible_missed_stop:"+pid,"You got as close as about "+Math.round(closest)+" m to "+placeName(p)+" and are now moving farther away. Check whether you missed your stop or took the wrong route.","overshoot:"+pid+":"+now);st.putState("approach_overshoot_alerted","1");}}\n\n    public static void mobilityAlert(Context c,String type,String message,String key)');p.write_text(s)
p=r/"MobilityTrackingService.java";s=p.read_text().replace("Persistent high-quality location collection for NullSignal journeys and place learning.","Persistent high-quality location collection for NullSignal commutes, journeys and place learning.");p.write_text(s)
try:(r/"ParkingInference.java").unlink()
except FileNotFoundError:pass
